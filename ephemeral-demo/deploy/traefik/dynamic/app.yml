http:
  routers:
    preview-app:
      rule: "Host(`demo.127.0.0.1.sslip.io`) || Host(`localhost`)"
      entryPoints: ["web"]
      service: preview-app

  services:
    preview-app:
      loadBalancer:
        servers:
          - url: "http://app:8080"

