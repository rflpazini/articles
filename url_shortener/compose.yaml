services:
  redis:
    image: redis:alpine
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - .dаta/redis:/root/redis
    environment:
      - REDIS_PASSWORD=my-password
      - REDIS_PORT=6379
